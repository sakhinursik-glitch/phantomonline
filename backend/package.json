{
  "name": "phantom-online-backend",
  "version": "1.0.0",
  "description": "PHANTOM ONLINE — production-ready REST API backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "migrate": "node src/migrations/001_init.js",
    "seed": "node src/migrations/002_seed.js",
    "setup": "npm run migrate && npm run seed",
    "reset": "node src/migrations/003_reset.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^13.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
